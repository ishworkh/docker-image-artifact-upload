{
  "name": "docker-image-artifact-upload",
  "version": "1.2.0",
  "description": "Github action to upload docker image as artifact to share between jobs",
  "scripts": {
    "build": "./node_modules/.bin/webpack --entry ./index.js --target node --output-path dist/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ishworkh/docker-image-artifact-upload"
  },
  "keywords": [
    "github",
    "actions",
    "docker",
    "image",
    "artifact",
    "upload"
  ],
  "author": "Ishwor Khadka",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ishworkh/docker-image-artifact-upload/issues"
  },
  "homepage": "https://github.com/ishworkh/docker-image-artifact-upload",
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.9",
    "docker-image-artifact": "/Users/ikhadka/Personal/docker-image-artifact"
  },
  "devDependencies": {
    "webpack": "^5.74",
    "webpack-cli": "^4.10"
  }
}
